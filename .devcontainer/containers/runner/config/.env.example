# GitHub repository URL for react-scuba
GITHUB_URL=https://github.com/DeanLuus22021994/react-scuba

# Runner token (get this from: https://github.com/DeanLuus22021994/react-scuba/settings/actions/runners/new)
# Note: Tokens expire after 1 hour, so generate a new one when setting up (already expired)
RUNNER_TOKEN=BOVDJEANEEFAVOEQP5TKKCTI642HQ

# Runner name (optional, defaults to react-scuba-runner)
RUNNER_NAME=react-scuba-runner

# Work directory (optional, defaults to /actions-runner/_work)
RUNNER_WORKDIR=/actions-runner/_work

# Labels for the runner (optional, defaults to self-hosted,linux,x64)
LABELS=self-hosted,linux,x64,react,scuba

# Runner group (optional, defaults to Default)
RUNNER_GROUP=Default
