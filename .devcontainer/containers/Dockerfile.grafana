# Gateway Monitoring Stack - Multi-Container Deployment
# This directory contains configuration for the Gateway monitoring stack
# The actual containers (nginx, prometheus, grafana) are deployed via docker-compose.mcp.yml
# This Dockerfile is a placeholder since Gateway uses pre-built images

FROM alpine:3.21
LABEL description="Gateway Monitoring Stack placeholder - actual deployment via docker-compose"
CMD ["echo", "Gateway monitoring stack deployed via docker-compose.mcp.yml"]