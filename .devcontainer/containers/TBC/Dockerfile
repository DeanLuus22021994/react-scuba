# TBC Monitoring Stack - Multi-Container Deployment
# This directory contains configuration for the TBC monitoring stack
# The actual containers (nginx, prometheus, grafana) are deployed via docker-compose.mcp.yml
# This Dockerfile is a placeholder since TBC uses pre-built images

FROM alpine:3.21
LABEL description="TBC Monitoring Stack placeholder - actual deployment via docker-compose"
CMD ["echo", "TBC monitoring stack deployed via docker-compose.mcp.yml"]