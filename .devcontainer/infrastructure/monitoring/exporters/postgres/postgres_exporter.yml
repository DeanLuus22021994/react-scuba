# ==============================================================================
# PostgreSQL Exporter Configuration
# ==============================================================================
# Purpose: Configuration for postgres_exporter metrics collection
# Reference: https://github.com/prometheus-community/postgres_exporter
# ==============================================================================

# Connection is managed via DATA_SOURCE_NAME environment variable
# This minimal config file is provided to suppress the warning message
