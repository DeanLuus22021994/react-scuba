# Node Exporter - Hardware and OS metrics
# Reference: https://github.com/prometheus/node_exporter

FROM prom/node-exporter:v1.7.0

LABEL maintainer="Dean Luus" \
  description="Prometheus Node Exporter for host metrics" \
  version="1.7.0"

# Disable health check to prevent connection reset issues
# The service will be monitored through prometheus scraping instead
# HEALTHCHECK --interval=300s --timeout=30s --start-period=60s --retries=2 \
#   CMD wget --quiet --tries=1 --timeout=10 --spider http://localhost:9100/ || exit 1
