# Node Exporter - Hardware and OS metrics
# Reference: https://github.com/prometheus/node_exporter
# v1.6.1 is the last stable version before go-kit logger verbosity bug in v1.7.0+

FROM prom/node-exporter:v1.6.1

LABEL maintainer="Dean Luus" \
  description="Prometheus Node Exporter for host metrics" \
  version="1.6.1"

# v1.6.1 does NOT have the broken pipe logging bug from v1.7.0+
# No need to suppress logs - this version doesn't log normal HTTP closures as errors
# Default CMD is sufficient (no --log.level override needed)
